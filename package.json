{
  "name": "token-auth-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev --local",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.1.0",
    "@cloudflare/workers-types": "^4.20240605.0",
    "@types/node": "^20.14.2",
    "typescript": "^5.0.4",
    "vitest": "1.3.0",
    "wrangler": "^3.60.3"
  }
}
